- Create new doctype - City
	Create new module - MetaMorpher
	"is custom" - Checked
	Continue
	In Settings, go to bottom and add field:
		Field: City Name
		Type: Data
		Autoname: field:city_name
	Update

- Open "role permission manager"
	Doctype: City, Role: System manager ... check Import

- Reload

- Customize form: Project
	Change "Naming" from "Details"
		Change "naming rule" to "by fieldname"
		In autoname, type "field:project_name"
		Update
	Create new section and move
		From fields, edit "Customer details", click on "Insert above"
			Label "Other details"
			TYpe "Section break"
			CLick "Collapseble"
			Move these fields under this section
				Series
				%Completion method
				%Complete
				Priority
				From template
				Expected start date
				Expected end date
				-- Add proper column break
			Update
	Move Customer/Client in main section
	Add below 3 fields below "Customer/Client":
		Label				Type	Options
		Project Year		Select	25/26/27	Mandatory
		City				Link	City		Mandatory
		Project Category	Select	Residential/Commercial		Mandatory
Reload

Settings → Server Scripts → New
	Name: create project id
	Type: DocType Event
	Reference document type: Project
	Event: Before Insert



# DO NOT import anything (Server Scripts are sandboxed)

year = doc.custom_project_year
city = doc.custom_city
category = doc.custom_project_category
client = doc.customer   # using Client (Customer doctype)

def clean(val):
    return val.replace(" ", "_") if val else "XXX"

year = clean(year)
city = clean(city)
category = clean(category)
client = clean(client)

# Count existing projects for this year
count = frappe.db.count(
    "Project",
    {
        "custom_project_year": year
    }
)

seq = str(count + 1).zfill(3)

project_name = f"{year}-{seq}-{client}-{city}-{category}"

doc.project_name = project_name
doc.name = project_name   # THIS IS THE FIX









Client Script (Live Preview)
📍 Settings → Client Scripts → New
Client Script Settings
Name: create project id
DocType: Project
apply to: form
Enabled: yes


frappe.ui.form.on("Project", {
    refresh(frm) {
        update_preview(frm);
    },
    custom_project_year: update_preview,
    custom_city: update_preview,
    custom_project_category: update_preview,
    customer: update_preview
});

function clean(val) {
    return val ? val.replaceAll(" ", "_") : "XXX";
}

function update_preview(frm) {
    if (frm.is_new()) {
        const year = clean(frm.doc.custom_project_year);
        const city = clean(frm.doc.custom_city);
        const cat  = clean(frm.doc.custom_project_category);
        const cli  = clean(frm.doc.customer);

        frm.set_value(
            "project_name",
            `${year}-XXX-${cli}-${city}-${cat}`
        );
    }
}



